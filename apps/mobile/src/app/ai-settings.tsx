import React from 'react';
import { AISettingsContainer } from '../features/settings/components/AISettingsContainer';

export default function AISettingsScreen() {
  return <AISettingsContainer />;
}
